#include <iostream>
using namespace std;
int main()
{

    cout << "关键字不可以被作为变量或者常量的名称" << endl;
    // 关键字不可以被作为变量或者常量的名称
    // 以下定义都是错误的写法
    /*
    int a = 10;
    int class=10;
    int int=10;
    int new=10;
    const int default=10;
    */

// 以下都是c++的关键字
   /*
    alignas alignof andB and_eqB asma auto bitandB bitorB bool break case catch char
    char8_tc char16_t char32_t class complB conceptc const const_cast constevalc
    constexpr constinitc continue co_awaitc co_returnc co_yieldc decltype default
    delete do double dynamic_cast else enum explicit exportc extern false float
    for friend goto if inline int long mutable namespace new noexcept notB not_eqB
    nullptr operator orB or_eqB private protected public register reinterpret_cast
    requiresc return short signed sizeof static static_assert static_cast struct
    switch template this thread_local throw true try typedef typeid typename union
    unsigned using virtual void volatile wchar_t while xorB xor_eq
   */
}